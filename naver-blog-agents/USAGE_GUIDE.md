# 네이버 블로그 자동화 에이전트 - 상세 사용 가이드

## 목차
1. [시작하기](#1-시작하기)
2. [CLI 명령어 상세](#2-cli-명령어-상세)
3. [콘텐츠 유형별 사용법](#3-콘텐츠-유형별-사용법)
4. [에이전트 팀 소개](#4-에이전트-팀-소개)
5. [워크플로우 이해하기](#5-워크플로우-이해하기)
6. [출력 파일 활용](#6-출력-파일-활용)
7. [프로그래밍 방식 사용](#7-프로그래밍-방식-사용)
8. [고급 설정](#8-고급-설정)
9. [문제 해결](#9-문제-해결)

---

## 1. 시작하기

### 1.1 사전 요구사항
- Node.js 18.0.0 이상
- npm 또는 yarn
- Google Gemini API 키

### 1.2 설치

```bash
# 프로젝트 디렉토리로 이동
cd naver-blog-agents

# 의존성 설치
npm install

# 환경 변수 파일 생성
cp .env.example .env
```

### 1.3 API 키 설정

`.env` 파일을 열어 Gemini API 키를 설정합니다:

```env
# 필수 - Google Gemini API 키
GEMINI_API_KEY=AIzaSy...your_key_here

# 선택 - 네이버 API (고급 검색 기능용)
NAVER_CLIENT_ID=your_naver_client_id
NAVER_CLIENT_SECRET=your_naver_client_secret

# 출력 설정
OUTPUT_DIR=./output
LOG_LEVEL=info
```

### 1.4 빌드

```bash
npm run build
```

### 1.5 첫 번째 콘텐츠 생성

```bash
npm run cli -- generate -t "노트북 구매 가이드" -T info
```

---

## 2. CLI 명령어 상세

### 2.1 generate - 콘텐츠 생성

메인 명령어로, 블로그 콘텐츠를 자동 생성합니다.

```bash
npm run cli -- generate [옵션]
```

#### 필수 옵션

| 옵션 | 단축 | 설명 | 예시 |
|------|------|------|------|
| `--topic` | `-t` | 콘텐츠 주제 | "강남 맛집 추천" |
| `--type` | `-T` | 콘텐츠 유형 | info, marketing, review, food |

#### 선택 옵션

| 옵션 | 단축 | 설명 | 기본값 |
|------|------|------|--------|
| `--keywords` | `-k` | 키워드 (쉼표 구분) | - |
| `--audience` | `-a` | 타겟 독자층 | - |
| `--tone` | - | 톤 (formal/casual/friendly) | friendly |
| `--context` | `-c` | 추가 컨텍스트 | - |
| `--output` | `-o` | 출력 디렉토리 | ./output/주제-날짜 |
| `--timeout` | - | 타임아웃 (ms) | 600000 |
| `--verbose` | - | 상세 로그 출력 | false |

#### 사용 예시

```bash
# 기본 정보성 콘텐츠
npm run cli -- generate -t "노트북 구매 가이드" -T info

# 키워드 포함 맛집 리뷰
npm run cli -- generate -t "강남역 맛집" -T food -k "강남역,점심,데이트"

# 타겟 독자 지정 마케팅 콘텐츠
npm run cli -- generate -t "새로운 스킨케어 브랜드" -T marketing -a "20-30대 여성"

# 상세 로그와 함께 제품 리뷰
npm run cli -- generate -t "갤럭시 S25 울트라 리뷰" -T review --verbose

# 추가 컨텍스트 포함
npm run cli -- generate -t "서울 카페 추천" -T food -c "인스타 감성, 사진 찍기 좋은 곳 위주로"

# 특정 출력 경로 지정
npm run cli -- generate -t "여행 준비물" -T info -o "./my-output/travel"
```

### 2.2 team - 에이전트 팀 정보

```bash
npm run cli -- team
```

8명의 에이전트 팀 정보를 출력합니다:
- 이름, 나이, 역할, 특징

### 2.3 types - 콘텐츠 유형 안내

```bash
npm run cli -- types
```

4가지 콘텐츠 유형에 대한 설명과 담당 작가 정보를 출력합니다.

### 2.4 도움말

```bash
npm run cli -- --help
npm run cli -- generate --help
```

---

## 3. 콘텐츠 유형별 사용법

### 3.1 정보성 (info) - 현우 선생님 담당

**적합한 주제:**
- How-to 가이드
- 개념 설명
- 비교 분석
- FAQ
- 튜토리얼

**예시 명령어:**
```bash
# 가이드 콘텐츠
npm run cli -- generate -t "초보자를 위한 주식 투자 가이드" -T info

# 비교 분석
npm run cli -- generate -t "아이폰 vs 갤럭시 비교" -T info -k "스마트폰,비교,2025"

# How-to
npm run cli -- generate -t "홈카페 만드는 방법" -T info -a "커피 입문자"
```

**출력 특징:**
- 논리적 구조
- 쉬운 설명
- 단계별 가이드
- "~입니다/~합니다" 문체

### 3.2 마케팅 (marketing) - 지은 언니 담당

**적합한 주제:**
- 브랜드 소개
- 협찬/광고 콘텐츠
- 프로모션 안내
- 이벤트 홍보
- 체험단 리뷰

**예시 명령어:**
```bash
# 브랜드 콘텐츠
npm run cli -- generate -t "새로 오픈한 성수동 카페" -T marketing

# 프로모션
npm run cli -- generate -t "여름 세일 안내" -T marketing -k "할인,이벤트,여름"

# 체험단 리뷰
npm run cli -- generate -t "신제품 화장품 체험 후기" -T marketing -c "협찬 제품, 솔직 리뷰"
```

**출력 특징:**
- 감성적 스토리텔링
- 자연스러운 CTA
- 혜택 중심 메시지
- "~해요/~거든요" 친근한 문체

### 3.3 제품리뷰 (review) - 태현 담당

**적합한 주제:**
- IT 기기 리뷰
- 가전제품 리뷰
- 서비스/앱 리뷰
- 비교 리뷰

**예시 명령어:**
```bash
# IT 기기
npm run cli -- generate -t "맥북 프로 M4 리뷰" -T review -k "맥북,애플,노트북"

# 가전제품
npm run cli -- generate -t "다이슨 청소기 후기" -T review

# 서비스 리뷰
npm run cli -- generate -t "넷플릭스 vs 티빙 비교" -T review -a "OTT 고민하는 사람"
```

**출력 특징:**
- 장단점 균형 분석
- 스펙 vs 실사용 비교
- 평점/추천도 포함
- "~더라고요/~해요" 경험담 문체

### 3.4 맛집리뷰 (food) - 하린 담당

**적합한 주제:**
- 맛집 리뷰
- 카페 리뷰
- 디저트/베이커리
- 배달 음식 리뷰

**예시 명령어:**
```bash
# 맛집 리뷰
npm run cli -- generate -t "홍대 파스타 맛집" -T food -k "홍대,파스타,데이트"

# 카페 리뷰
npm run cli -- generate -t "연남동 디저트 카페" -T food -c "인스타 감성, 케이크 맛집"

# 배달 음식
npm run cli -- generate -t "야식 추천 배달 맛집" -T food -a "자취생"
```

**출력 특징:**
- 오감 묘사
- 분위기/서비스 포함
- 방문 팁 제공
- "~했어요/~인 것 같아요" 감성 문체

---

## 4. 에이전트 팀 소개

### 4.1 기획 단계

#### 📋 민준 팀장 (기획자)
- **나이:** 38세
- **성격:** 차분하고 전략적인 베테랑
- **역할:**
  - 콘텐츠 방향 설정
  - 타겟 독자 분석
  - 아웃라인 설계
  - 작가 배정
- **말투:** "전체적인 방향은~", "독자분들이 원하는 건~"

### 4.2 리서치 단계

#### 🔍 수빈 (리서처)
- **나이:** 29세
- **성격:** 꼼꼼한 팩트체커
- **역할:**
  - 정보 수집
  - 팩트체크
  - 경쟁 분석
  - 트렌드 파악
- **말투:** "확인해본 결과~", "출처에 따르면~"

### 4.3 작성 단계

#### 📚 현우 선생님 (정보성 작가)
- **나이:** 45세
- **담당:** info 유형
- **특징:** 쉬운 설명의 달인, 교육적 접근
- **말투:** "쉽게 말하면~", "이해하기 쉽게 정리하면~"

#### ✨ 지은 언니 (마케팅 작가)
- **나이:** 33세
- **담당:** marketing 유형
- **특징:** 감성 스토리텔러, 트렌드 민감
- **말투:** "솔직히 말하면~", "이건 꼭 알아야 해요!"

#### ⭐ 태현 (제품리뷰 작가)
- **나이:** 27세
- **담당:** review 유형
- **특징:** 솔직한 장단점 분석, 실용주의
- **말투:** "실제로 써보니까~", "가성비로 따지면~"

#### 🍽️ 하린 (맛집리뷰 작가)
- **나이:** 31세
- **담당:** food 유형
- **특징:** 오감 묘사 전문, 감성적 표현
- **말투:** "입에 넣는 순간~", "분위기가 정말~"

### 4.4 편집/SEO 단계

#### ✏️ 서연 실장 (편집자)
- **나이:** 40세
- **역할:**
  - 맞춤법 교정
  - 문장 다듬기
  - 톤 일관성 검토
- **말투:** "이 부분은 조금 다듬으면~", "전체적인 톤을 맞춰볼게요"

#### 🎯 준서 (SEO 전문가)
- **나이:** 35세
- **역할:**
  - 키워드 최적화
  - 제목/메타 SEO
  - 태그 추천
- **말투:** "검색량 데이터를 보면~", "이 키워드로 가면~"

---

## 5. 워크플로우 이해하기

### 5.1 전체 흐름

```
[입력] → [기획] → [리서치] → [작성] → [편집] → [SEO] → [포맷팅] → [출력]
         민준     수빈       작가     서연     준서
```

### 5.2 단계별 설명

| 단계 | 담당 | 설명 | 소요시간 |
|------|------|------|----------|
| 1. 기획 | 민준 팀장 | 콘텐츠 전략, 아웃라인 설계 | ~30초 |
| 2. 리서치 | 수빈 | 정보 수집, 팩트체크 | ~60초 |
| 3. 작성 | 담당 작가 | 초안 작성 | ~90초 |
| 4. 편집 | 서연 실장 | 맞춤법, 톤 검토 | ~30초 |
| 5. SEO | 준서 | 키워드, 제목 최적화 | ~20초 |
| 6. 포맷팅 | 시스템 | HTML, MD, JSON 변환 | ~5초 |

### 5.3 에러 처리

- 각 단계는 최대 3회 재시도
- 단계별 타임아웃: 3분
- 전체 타임아웃: 10분

---

## 6. 출력 파일 활용

### 6.1 출력 파일 구조

```
output/강남-맛집-추천-2025-01-30/
├── content.html      # 네이버 스마트에디터용
├── content.md        # 마크다운
├── content.json      # 구조화된 JSON
└── metadata.json     # 메타데이터
```

### 6.2 content.html 사용법

**네이버 블로그에 붙여넣기:**
1. `content.html` 파일을 텍스트 에디터로 열기
2. 전체 내용 복사 (Ctrl+A, Ctrl+C)
3. 네이버 블로그 글쓰기 → HTML 모드로 전환
4. 붙여넣기 (Ctrl+V)
5. 에디터 모드로 돌아가서 확인

### 6.3 content.md 사용법

**마크다운 편집기 사용:**
- Notion, Obsidian 등에서 바로 사용
- GitHub, GitLab README로 활용
- 추가 편집 후 변환

### 6.4 content.json 활용

**프로그래밍 연동:**
```javascript
const content = require('./output/강남-맛집/content.json');

console.log(content.title);       // 제목
console.log(content.sections);    // 섹션 배열
console.log(content.seo.tags);    // SEO 태그
```

### 6.5 metadata.json 정보

```json
{
  "title": "콘텐츠 제목",
  "seoTitle": "SEO 최적화 제목",
  "metaDescription": "메타 설명",
  "tags": ["태그1", "태그2"],
  "metadata": {
    "contentType": "food",
    "writer": "하린",
    "wordCount": 2500,
    "readingTime": 5,
    "seoScore": 85
  }
}
```

---

## 7. 프로그래밍 방식 사용

### 7.1 기본 사용법

```typescript
import { createBlogContent } from 'naver-blog-agents';

async function main() {
  const result = await createBlogContent({
    topic: '강남 맛집 추천',
    type: 'food',
    keywords: ['강남역', '데이트', '맛집'],
    targetAudience: '20-30대 직장인',
    tone: 'friendly',
  });

  if (result.success && result.content) {
    // 네이버 HTML
    console.log(result.content.formats.naverHtml);

    // 마크다운
    console.log(result.content.formats.markdown);

    // SEO 정보
    console.log(result.content.seoTitle);
    console.log(result.content.tags);
  }
}

main();
```

### 7.2 이벤트 리스너 사용

```typescript
import { generateBlogContent } from 'naver-blog-agents';

const result = await generateBlogContent(
  {
    topic: '노트북 추천',
    type: 'review',
  },
  {
    totalTimeout: 300000, // 5분
    onEvent: (event) => {
      switch (event.type) {
        case 'stage_started':
          console.log(`📍 ${event.stage} 단계 시작`);
          break;
        case 'stage_completed':
          console.log(`✅ ${event.stage} 완료 (${event.duration}ms)`);
          break;
        case 'error':
          console.error(`❌ 오류: ${event.error.message}`);
          break;
      }
    },
  }
);
```

### 7.3 개별 에이전트 사용

```typescript
import {
  getPlannerAgent,
  getResearcherAgent,
  getWriterAgent
} from 'naver-blog-agents';

// 기획자만 사용
const planner = getPlannerAgent();
const planResult = await planner.plan({
  topic: '여행 준비물',
  type: 'info',
});

console.log(planResult.plan.title);
console.log(planResult.plan.outline);

// 작가 직접 선택
const writer = getWriterAgent('food');  // 하린
// const writer = getWriterAgent('info');  // 현우 선생님
```

### 7.4 포맷터 직접 사용

```typescript
import {
  formatToNaverHtml,
  formatToMarkdown,
  formatToJSON
} from 'naver-blog-agents';

// 직접 포맷 변환
const html = await formatToNaverHtml(editedContent, seoData);
const md = await formatToMarkdown(editedContent, seoData);
const json = await formatToJSON(editedContent, seoData);
```

---

## 8. 고급 설정

### 8.1 모델 설정

`src/utils/config.ts`에서 모델 변경 가능:

```typescript
export const MODEL_IDS: Record<ModelType, string> = {
  opus: 'gemini-1.5-pro',      // 고성능 (복잡한 작업)
  sonnet: 'gemini-1.5-flash',  // 균형 (기본)
  haiku: 'gemini-1.5-flash-8b', // 빠른 (간단한 작업)
};
```

### 8.2 에이전트별 모델 할당

```typescript
const defaultConfig = {
  models: {
    default: 'sonnet',
    agents: {
      planner: 'sonnet',     // 기획자
      researcher: 'sonnet',  // 리서처
      writers: 'sonnet',     // 작가들
      editor: 'sonnet',      // 편집자
      seoExpert: 'haiku',    // SEO (빠른 처리)
    },
  },
};
```

### 8.3 프롬프트 커스터마이징

`prompts/` 디렉토리의 마크다운 파일을 수정하여 에이전트 행동 변경:

```
prompts/
├── planner.md          # 기획자 프롬프트
├── researcher.md       # 리서처 프롬프트
├── writers/
│   ├── info-writer.md      # 정보성 작가
│   ├── marketing-writer.md # 마케팅 작가
│   ├── review-writer.md    # 제품리뷰 작가
│   └── food-writer.md      # 맛집리뷰 작가
├── editor.md           # 편집자 프롬프트
└── seo-expert.md       # SEO 전문가 프롬프트
```

### 8.4 로그 레벨 설정

`.env` 파일에서 조절:

```env
LOG_LEVEL=debug   # 모든 로그
LOG_LEVEL=info    # 정보 이상 (기본)
LOG_LEVEL=warn    # 경고 이상
LOG_LEVEL=error   # 에러만
```

---

## 9. 문제 해결

### 9.1 일반적인 오류

#### API 키 오류
```
Error: GEMINI_API_KEY 환경 변수가 설정되지 않았습니다.
```
**해결:** `.env` 파일에 올바른 API 키 설정

#### 타임아웃 오류
```
Error: 작성 단계 시간 초과
```
**해결:** `--timeout` 옵션으로 시간 늘리기
```bash
npm run cli -- generate -t "주제" -T info --timeout 900000
```

#### 모델 오류
```
Error: Model not found
```
**해결:** `config.ts`에서 올바른 Gemini 모델명 확인

### 9.2 콘텐츠 품질 문제

#### 내용이 너무 짧음
- 키워드 추가: `-k "키워드1,키워드2"`
- 컨텍스트 추가: `-c "상세한 내용 요청"`

#### 톤이 맞지 않음
- `--tone` 옵션 변경: `formal`, `casual`, `friendly`
- 프롬프트 파일 수정

### 9.3 디버깅

```bash
# 상세 로그 출력
npm run cli -- generate -t "테스트" -T info --verbose

# 로그 레벨 변경 (.env)
LOG_LEVEL=debug
```

### 9.4 성능 최적화

- SEO 에이전트는 `haiku` 모델 사용 (빠름)
- 간단한 콘텐츠는 `--timeout` 줄이기
- 복잡한 주제는 키워드/컨텍스트 상세히 제공

---

## 부록: 빠른 참조

### 명령어 요약

```bash
# 기본 생성
npm run cli -- generate -t "주제" -T [info|marketing|review|food]

# 전체 옵션
npm run cli -- generate \
  -t "주제" \
  -T food \
  -k "키워드1,키워드2" \
  -a "타겟 독자" \
  --tone friendly \
  -c "추가 컨텍스트" \
  -o "./output/custom" \
  --verbose

# 정보 확인
npm run cli -- team    # 팀 정보
npm run cli -- types   # 유형 정보
npm run cli -- --help  # 도움말
```

### 콘텐츠 유형 선택 가이드

| 주제 예시 | 추천 유형 | 담당 작가 |
|----------|----------|----------|
| ~하는 방법, 가이드 | info | 현우 선생님 |
| 브랜드, 협찬, 이벤트 | marketing | 지은 언니 |
| IT, 가전, 서비스 리뷰 | review | 태현 |
| 맛집, 카페, 음식 | food | 하린 |
